{
  "private": true,
  "scripts": {
    "elm-test": "elm-test",
    "test": "elm-verify-examples && elm-test"
  },
  "devDependencies": {
    "elm": "^0.18.0",
    "elm-format": "^0.6.1-alpha",
    "elm-test": "^0.18.6",
    "elm-verify-examples": "^1.0.1"
  }
}
